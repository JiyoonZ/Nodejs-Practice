// 덧셈, 뺄셈, 곱셈

//덧셈
const add = (a,b) => a+b;

//뺄셈
const sub = (a,b) => a-b;

//덧셈
const mul = (a,b) => a*b;

const num = 123;
const str = "HELLO"

module.exports = {
  add2: add,
  sub2: sub,
  mul2: mul,
  num : num,
  str : str,
}
